name: haraya_app
description: Haraya Homes Flutter App

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10+1
  google_fonts: ^6.2.1
  http: ^1.2.2
  url_launcher: ^6.3.0
  shared_preferences: ^2.3.2
  file_picker: ^6.1.1
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.7
  google_maps_flutter: ^2.8.0
  geolocator: ^10.0.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  webview_flutter: ^4.8.0
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
